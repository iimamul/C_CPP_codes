#include<stdio.h>

int main(){
    float number,answer;
    printf("give the number");
    scanf("%f",&number);
    answer=number*number;
    printf("the square of the number is%f",answer);
    return 0;
    }
